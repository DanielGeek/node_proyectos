{
    "compilerOptions": {
        // [outDir] luego del compilado se guardara en esta carpeta
        "outDir": "./dist/",
        // [target]:[es2019] usar las últimas funciones de js, nota: verificar si la version de tu node es compatible https://node.green/#ES2015
        "target": "es2019",
        // [module]:[commonjs] on esto se puede usar import en vez de require y al compilar se adaptara a la versión en producción
        "module": "CommonJS",
        // [allowJs]:[true] permitir el uso de [CommonJS]
        "allowJs": true,
    }
}